import type { NextPage } from 'next';
import MainHeader from '../components/MainHeader';

const Home: NextPage = () => {
  return (
    <div>
      <MainHeader />
    </div>
  );
};

export default Home;
